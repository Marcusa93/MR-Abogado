{
  "name": "alba-guerra-local",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 0.0.0.0 --port 3001",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0 --port 3002"
  }
}
